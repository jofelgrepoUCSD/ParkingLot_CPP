#ifndef ENUMS_H
#define ENUMS_H

enum VehicleType {
   MOTORBIKE = 1,
   CAR = 2,
   TRUCK = 3,
};

enum ParkingSpotType {
    MEDIUM = 1,
    LARGE = 2,
    SMALL = 3
};

enum TicketStats {
    ACTIVE = 1,
    PAID = 2,
    LOST = 3
};

#endif  