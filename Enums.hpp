#ifndef ENUMS_H
#define ENUMS_H

enum VehicleType {
   CAR = 1,
   TRUCK = 2,
   VAN = 3,
   MOTORBIKE = 4
};

enum ParkingSpotType {
    MEDIUM = 1,
    LARGE = 2,
    SMALL = 3
};

enum TicketStats {
    ACTIVE = 1,
    PAID = 2,
    LOST = 3
};

#endif  